import React from 'react';
import { Box, Typography } from '@mui/material';

const ProjectPlanningTest: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Project Planning Test</Typography>
      <Typography>This is a test component to check if the issue is with the main component.</Typography>
    </Box>
  );
};

export default ProjectPlanningTest;
